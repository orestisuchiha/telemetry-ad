dataset: skab
train_file: Datasets/SKAB/anomaly-free/anomaly-free.csv
training:
  threshold_percentile: 99.0
test_files:
  primary: Datasets/SKAB/valve1/1.csv
  optional_secondary: Datasets/SKAB/valve1/2.csv
separator: ";"

split_name: anomalyfree_vs_valve1_1

default_model_set:
  - zscore
  - iforest
  - lstm_ae
  - cnn_ae

inference:
  threshold_calibration:
    enabled: true
    warmup_windows: 300
    suppress_during_warmup: true
    default:
      mode: artifact
      percentile: 99.5
    models:
      zscore:
        mode: warmup_percentile
        percentile: 10.0
      iforest:
        mode: warmup_percentile
        percentile: 50.0
      lstm_ae:
        mode: warmup_percentile
        percentile: 75.0
      cnn_ae:
        mode: warmup_percentile
        percentile: 60.0
